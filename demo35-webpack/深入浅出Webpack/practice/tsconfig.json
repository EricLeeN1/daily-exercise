{
    "compilerOptions": {
        "module": "es2015",
        // 编译出的代码采用的模块规范
        "target": "es5",
        // 编译出的代码采用 ES 的哪个版本
        "sourceMap": true,
        // 输出 SourceMap 方便调试
        "importHelpers": true,
        // 减少代码冗余,
        "jsx": "react",
        // 开启jsx,支持React,
        "strict": true,
        // 开启严格模式，这可以对 `this` 上的数据属性进行更严格的推断,
        "moduleResolution": "node"
    },
    "exclude": [
        // 不编译这些目录里的文件
        "node_modules"
    ]
}