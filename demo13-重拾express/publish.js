window.onload = function () {
    var time = 1000;
    aVal = document.getElementById('a').value();
    var time = setInterval(function () {
        if (a.length > 9) {
            ajax(aval);
        }
    }, time);
}